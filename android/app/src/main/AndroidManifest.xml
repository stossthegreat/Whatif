<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- INTERNET is added automatically to debug builds only — without it
         here, the RELEASE build cannot reach the backend at all. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- ADVERTISING ID — removed on purpose.
         firebase_analytics pulls in play-services-measurement, which merges
         com.google.android.gms.permission.AD_ID into the manifest whether or
         not anything uses it. Its presence is what makes Play ask you to fill
         in the Advertising ID declaration, and it drags the ad identifier into
         the Data safety form as collected data.
         Rivler runs no ads and does no install attribution, so it has nothing
         to declare. Stripping it here means the honest answer to Play's
         "Does your app use advertising ID?" is simply NO.
         The cost is Firebase Analytics demographics and interest reporting.
         If ads or attribution are ever added, delete this line and answer YES. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.front" android:required="false"/>
    <application
        android:label="Rivler"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <!-- PREDICTIVE BACK. Targeting API 36 makes this true by default on
             Android 16, where onBackPressed stops being called and KEYCODE_BACK
             stops being dispatched. Declared explicitly so the behaviour is the
             same on Android 13-15 instead of silently changing with the OS
             version. Safe here because nothing in this app intercepts the system
             back event: there is no WillPopScope and no PopScope anywhere in
             lib/, only Navigator.maybePop() wired to in-app back buttons, which
             is ordinary route navigation and unaffected. If a screen ever needs
             to intercept back (an "are you sure?" on a live call, say), it must
             use PopScope — WillPopScope does not work under predictive back. -->

        <!-- LARGE SCREENS — TEMPORARY OPT-OUT, EXPIRES AT API 37.
             For apps targeting API 36, Android ignores screenOrientation,
             resizableActivity, minAspectRatio, maxAspectRatio and
             setRequestedOrientation() on any display >= sw600dp. Our
             SystemChrome.setPreferredOrientations([portraitUp]) in main.dart
             goes through setRequestedOrientation(), so on a tablet or an
             unfolded foldable this app would rotate and resize freely into
             layouts that were only ever designed for a portrait phone.
             This property keeps the old compatibility-mode behaviour while
             that stays true.

             This is deliberate debt, not an oversight. Google removes the
             opt-out entirely when we target API 37, so the real fix — layouts
             that adapt to landscape and to sw600dp+ — has to land before then.
             Delete this block on the same day that work ships. -->
        <property
            android:name="android.window.PROPERTY_COMPAT_ALLOW_RESTRICTED_RESIZABILITY"
            android:value="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
